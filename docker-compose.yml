version: '3.8'

services:
  webhook-logger:
    build: .
    ports:
      - "5000:5000"
    restart: unless-stopped
    volumes:
      - ./data:/app/data:rw
    environment:
      - FLASK_ENV=production
